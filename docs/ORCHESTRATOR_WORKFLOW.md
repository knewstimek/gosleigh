# 오케스트레이터 작업 방식 (보류)

> **상태: 보류 (parked).** 이 문서는 한때 프로젝트 `CLAUDE.md`의 작업 규칙이었던
> "오케스트레이터 + 서브에이전트 위임" 모델을 보존한다. 현재는 이 방식을 적극적으로
> 쓰지 않기로 하여 `CLAUDE.md`에서 빼냈다. 다시 이 모델로 작업하기로 하면 아래 내용을
> `CLAUDE.md`로 되돌린다.
>
> **갱신 (2026-07-03):** 이 supervisor + 서브에이전트 위임 모델이 다시 채택됐다. 아래 규칙을 다시 적용한다.

## 위임 모델

- 오케스트레이터는 프로덕션 코드를 직접 구현하지 않는다. 구현 작업은 서브에이전트 소관이다.
- 오케스트레이터는 조율, 범위 분리, 진행상황 회수, 충돌 확인, 문서화, 리뷰, 통합 판단, 최종 검증만 맡는다.
- 구현 작업에는 소넷이나 mini 모델을 사용한다. 예외는 좁은 읽기 전용 탐색뿐이며, 이유를 명시한다.

## 운영 예외

- 컨텍스트 압축 무한 반복 방지: 서브에이전트가 매 턴 압축-재기동을 반복하면 오케스트레이터가 직접 구현해도 된다. 이유를 주석으로 남길 것.
